<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_URL" value="http://localhost"/>
        <!-- First host is the technical host (admin/API/Horizon — from APP_URL);
             the rest are short-link domains the resolver answers on. -->
        <env name="APP_HOST" value="localhost,lnk.test,example.com"/>
        <env name="APP_TIMEZONE" value="UTC"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_DATABASE" value="testing"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
        <!-- Geo is off by default in tests: no license key means no download is
             ever dispatched (a real key in a dev/CI .env would otherwise reach
             MaxMind mid-test), and an absent database path keeps the locator
             inert. Tests that exercise geo set config('geo.*') explicitly. -->
        <env name="GEOIP_LICENSE_KEY" value=""/>
        <env name="GEOIP_DATABASE_PATH" value="storage/framework/testing/geoip-absent.mmdb"/>
    </php>
</phpunit>
