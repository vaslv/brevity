# Принятые решения и осознанные риски

Журнал долговечных решений (ADR-лайт). Сюда попадает то, что нельзя
вывести из кода: «почему так», принятые компромиссы и остаточные риски.
Полные тексты аудита 2026-06-05 и ревью 2026-06-01 удалены из репозитория
(есть в git-истории: `docs/AUDIT_2026-06.md`, `docs/CODE_REVIEW.md`);
всё существенное из них — здесь.

## Архитектурные и продуктовые решения

- **Все `User` — полностью доверенные администраторы.** Ролевой модели нет
  намеренно; создание учётки = выдача полного доступа (включая Horizon и
  выпуск токенов). Escape hatch при необходимости — флаг `users.is_admin`.
  Детали: [04-admin.md](./04-admin.md).
- **Soft-deleted ссылка = отключена**: публичный резолв даёт 404, админ
  видит trashed. Клики и колбеки — исторические факты, **переживают
  ссылку** (в джобах `withTrashed()`, FK `restrictOnDelete`).
- **Связи моделей — только трейтами** в `app/Models/Relations/`, даже
  однострочные (осознанно вопреки рекомендации аудита «заинлайнить»).
- **Клик засчитывается на рендере интерстишала** (delayed/manual) и для
  ботов/prefetch — продуктовое решение; боты помечаются флагом
  (этап 1 [планов](./07-plans.md)), а не выбрасываются.
- **Футер логина рендерится как HTML без экранирования** — принято by
  design: `content.footer` задаёт только доверенный админ. Пересмотреть,
  если появятся недоверенные операторы.
- **Генерация кода в `Link::booted()`** — принято как идиоматичное
  (аудит предлагал перенос в `LinkCreator`; отклонено).
- **IP-pinning в колбеках (TOCTOU / DNS-rebinding) не реализован
  намеренно**: `callback_url` задаёт только админ, follow-redirect
  отключён, а чистая реализация (CURLOPT_RESOLVE) нетестируема через
  `Http::fake`. Включить при переходе на пользовательские `callback_url`.
- **trustProxies = приватные подсети** (`10/8`, `172.16/12`, `192.168/16`),
  не `*`. В проде желательно сузить до реального прокси. Остаточный риск:
  `172.16/12` покрывает все docker-bridge-сети.
- **`HASHIDS_SALT` независим от `APP_KEY`** — ротация ключа приложения не
  меняет коды ссылок. `links.code` — `varchar(16)`: потолок Hashids снят
  на весь диапазон bigint (9-символьный код начинается с id ~52,5 млрд).
- **Корневой маршрут `GET /{code}`** ограничен constraint'ом по
  алфавиту/длине кода; проверено, что Hashids не кодирует id в
  зарезервированные слаги Filament (`login`, `services`, …). Остаточный
  риск затенения — только при смене алфавита.
- **`round_robin` доменов — намеренно без курсора**: порядок выводится из
  истории ссылок (`MAX(links.created_at)`), поэтому самокорректируется при
  добавлении/удалении доменов. Под нагрузкой два одновременных запроса
  могут получить один домен — принятый best-effort компромисс; строгая
  гарантия потребовала бы таблицу-курсор с `lockForUpdate`. Статистика
  ротации глобальная (по всем сервисам): домен — общий ресурс.
- **Проверено на рантайме** (не менять «на всякий случай»):
  `firstOrCreate` в Laravel 13 race-safe через `createOrFirst` + savepoint;
  Filament v5 `unique()` игнорирует текущую запись на edit по умолчанию.

## Продуктовые решения 2026-07-11 (разбор каталога competitor)

Обоснования — [10-competitor-catalog-review.md](./10-competitor-catalog-review.md).

- **Колбеки по бот-кликам отправляются всегда, с меткой `is_bot` в корне
  payload** (клиентский ключ `is_bot` перекрывается). Партнёр решает сам;
  ложноположительная детекция не теряет конверсию.
- **A/B sticky — детерминированный хэш `(ip, user_agent, link_id)`**
  против весов; без cookie и состояния. Компромисс: смена IP/браузера
  может дать другой вариант.
- **IP хранится сырым + плановая чистка** (ориентир 90 дней): сырой IP
  нужен антифроду и колбекам партнёров; бессрочное хранение — нет.
- **Кастомные слаги не делаем**: ссылки создаются программно, инвариант
  «код из Hashids, коллизий нет» сохраняется. Вернуться при явном запросе
  клиента.

## Продуктовые решения 2026-07-12 (этап 2 плана)

- **`max_clicks` считает все клики, включая ботов** — лимит
  детерминирован и объясним клиенту; бот-составляющая видна в разрезе
  счётчиков, спорные случаи разбираются по метке `is_bot` в колбеках.
- **`valid_since` добавляем** — симметрично `valid_until`; до старта
  ссылка неотличима от несуществующей (404, ни клика, ни колбека).
  Воркараунд через `after_date`-условия остаётся валидным, но поле
  дешевле и явнее.
- **Версионирование API: вводим `/api/v1`.** RFC 7807 Problem Details и
  вся новая функциональность (PATCH/GET ссылок, новые поля) — только под
  `/api/v1`. Legacy-маршруты `/api/*` замораживаются: прежний формат
  ошибок, прежний набор возможностей, помечены deprecated в 03-api.md;
  удаление — отдельным решением после миграции клиентов.
  *Уточнение 2026-07-12 (кросс-ревью, r13):* заморозка = пути, формат
  ошибок и отсутствие ломающих изменений. Контроллеры/Request/Resource
  общие с v1, поэтому legacy-ответы получают новые **аддитивные** поля
  (`conditions`, `variants`, lifecycle) и общую валидацию входов — это
  принято осознанно; форк классов ради байт-в-байт заморозки отклонён.

## Реестр находок аудита и ревью (для регресс-тестов)

Регресс-тесты ссылаются на эти ID (`Regression for docs/08-decisions.md
(аудит) — H2` и т.п.). Все находки закрыты или приняты; полные тексты —
в git-истории.

### Ревью 2026-06-01 (`docs/CODE_REVIEW.md` в истории)

| ID | Суть | Итог |
|---|---|---|
| C1 | `trustProxies('*')` → спуф IP, обход лимитера | Фикс: доверие только приватным подсетям |
| M1 | «`firstOrCreate` не race-safe» | Снято: race-safe через `createOrFirst` |
| M2 | `RecordClickJob` не идемпотентен → дубли кликов/колбеков | Фикс: `clicks.uuid` + `firstOrCreate` |
| M3 | SSRF колбеков: follow-redirect, TOCTOU | Фикс: редиректы откл.; пиннинг — принятый риск |
| M4 | «`SettingForm.key` ломает edit» | Снято: Filament игнорирует текущую запись |
| M5 | Хрупкий приоритет `GET /{code}` vs Filament/`/up` | Фикс: constraint на код + guard-тест |
| M6 | Нет ролей — любой юзер = админ | By design (см. выше) |
| M7 | Нет индекса `clicks(created_at)`; не-sargable бейджи | Фикс: индекс + range-фильтры |
| m1 | Мёртвая связь `Callback::link` | Убрана |
| m2 | `rules.*.url` без ограничения схемы | Фикс: `url:http,https` |
| m3 | Нет верхних границ валидации (`rules`, `callback_data`) | Фикс: лимиты + стрип лишних ключей |
| m4 | Токены бессрочные, все abilities | Фикс: ability `links:create`, срок, prune |
| m5 | Дубль `priority` в форме правил → сырой DB-error | Фикс: валидация уникальности |
| m6 | Сырые ошибки БД в админке (дубль условия и др.) | Фикс: дедуп/дружелюбные ошибки |
| m7 | ForceDelete ссылки с кликами → FK-ошибка | Убран |
| m8 | `email_verified_at` не в `$fillable` | Фикс |
| m9 | Таймзона в агрегации кликов | Консистентно (UTC), комментарий |
| m10 | Фильтр кликов грузит все ссылки | Фикс: searchable relationship |
| m11 | Off-by-one в backoff колбеков (5 задержек на 5 попыток) | Фикс: 5 попыток / 4 интервала |
| m12 | Ссылка из админки без правил → 404 | Фикс: нотификация после create |
| m13 | `callback_data` в админке — плоская карта | Фикс: JSON-редактор, round-trip |
| m14 | Лишняя транзакция в `ClickRecorder` | Убрана |

### Аудит 2026-06-05 (`docs/AUDIT_2026-06.md` в истории)

| ID | Суть | Итог |
|---|---|---|
| C1 | Потеря кликов: btree-переполнение от многобайтовых Referer/UA | Фикс: обрезка по байтам (`mb_strcut`) |
| C2 | Секреты запечены в Docker-образ (нет `.dockerignore`) | Фикс + ротация кредов |
| H1 | Контракт: `condition:null` отдавался как `{type:null,…}` | Фикс: null-гард в `RuleResource` |
| H2 | Нет unique на `callbacks.click_id` → дубли вебхуков | Фикс: unique-индекс |
| H3 | Stored XSS через футер на странице логина | Принято by design (см. выше) |
| H4 | `HASHIDS_SALT` по умолчанию = `APP_KEY` | Фикс: независимая соль |
| H5 | Нет индексов FK на `links` | Фикс |
| H6 | Нет `migrate` на деплое; почта в `log` | Фикс |
| H7 | Потолок `varchar(8)` для кода | Фикс: `varchar(16)` |
| H8 | `POST /api/links` без throttle | Фикс: 120/мин на сервис |
| M1 | `title` `max:255` vs `varchar(64)` | Фикс: `max:64` |
| M2/M3 | Soft-delete ссылки терял клик / ронял колбек | Фикс: `withTrashed()` в джобах |
| M4 | Зависший `Pending`-колбек без sweeper'а | Фикс: `callbacks:dispatch-pending` |
| M5 | Horizon-gate = любой аутентифицированный | Фикс: гейт через `canAccessPanel` |
| M6 | Токены без TTL/prune | Фикс |
| M7 | Сырые 500 Postgres в Filament | Фикс: `->before()`-гарды |
| M8 | ForceDelete на EditLink | Убран |
| M9 | Footgun: очередь `database` vs Horizon `redis` | Фикс: fail-fast ассерт |
| M10 | Схема URL не перепроверялась на чтении | Фикс |
| M11 | `LOG_LEVEL=debug` в проде, эфемерные логи | Фикс: warning + stderr |
| M12 | Генерация кода в boot-хуке | Принято (идиоматично) |
| M13 | Одна очередь `default` — колбеки голодоморят клики | Фикс: отдельная очередь `callbacks` |
| M14 | Ре-валидация условия на каждом резолве | Фикс: парсинг на чтении |
| M15 | Bulk-delete пользователей без гарда | Фикс: нельзя удалить себя/последнего |
| Low | Мёртвый код, стили, резолвер словарей, CI-блоки A/B, UX | Закрыты; «трейт на связь» — отклонено (см. выше) |

### Кросс-ревью перед пушем 2026-07-12 (этапы 1–3)

Диапазон `origin/main..HEAD` (30 коммитов) прошёл 8 независимых проходов
Claude (адверсариальный + специалисты: security, миграции, тесты,
API-контракт, производительность, сопровождаемость) и ревью Codex.
Миграции этапов на тот момент не были задеплоены, поэтому правились на
месте (без repair-миграций).

| ID | Суть | Итог |
|---|---|---|
| r1 | `->constrained()->…->index()` НЕ создаёт индекс — затирает ИМЯ FK-констрейнта в `"1"`, ломая и rollback (`dropConstrainedForeignId` ищет конвенционное имя); индексов `clicks.rule_variant_id` и `rule_variants.rule_id` не существовало (подтверждено psql) | Фикс: `$table->index(...)` отдельным вызовом; dev-БД починена вручную |
| r2 | Нет индекса `clicks.ip_address_id`: RI-проверка каждого DELETE из `ip_addresses` в `ips:prune` = seq scan clicks на строку | Фикс: индекс |
| r3 | Detach-фаза `ips:prune` ежедневно перечитывала всю историю до cutoff | Фикс: частичный индекс `clicks(created_at) WHERE ip_address_id IS NOT NULL` |
| r4 | Гонка PATCH: атрибуты читались до `lockForUpdate`, два конкурентных PATCH могли закоммитить скрещенное окно since>until | Фикс: `refresh()` под локом + тест |
| r5 | `parse_str` манглит `.`/пробел/`[` в именах query-параметров → `{{click.query.sub.id}}` навсегда отдавал `''` | Фикс: ручной разбор пар, имена литеральные |
| r6 | `Condition` нёс `HasManyRules` по удалённой колонке `rules.condition_id` — SQL-ошибка при первом обращении | Фикс: трейт убран (вызовов не было) |
| r7 | Бот-детекция гоняла полный OS/client/device-парс для НЕ-ботов; комментарий обещал обратное | Фикс: `Parser\Bot` напрямую + `discardDetails()` |
| r8 | `device`-условие парсило сырой UA (без капа) по разу на каждое device-правило за редирект | Фикс: кап 2000 байт + одноэлементный мемо |
| r9 | 404-title из `getMessage()` утёк бы класс модели при будущем `findOrFail` под v1 | Фикс: фиксированный title для 404 |
| r10 | `conditions()` без ORDER BY: недетерминированный порядок массива и «первого» условия для legacy `condition` | Фикс: `orderBy('conditions.id')` |
| r11 | Повторный `detect-bots` молча оставлял счётчики штейл | Фикс: warn «run clicks:rebuild-counters» при изменениях |
| r12 | `condition_rule.condition_id` без индекса (restrict-check удаления условия) | Фикс: индекс |
| r13 | Legacy `/api/*` не «байт-в-байт»: общие классы дали legacy новые поля ответов и общую валидацию | Принято решением (см. «/api/v1» выше) |
| r14 | `max_clicks` при конкурентном всплеске превышается: проверка идёт до асинхронной записи клика (Codex) | Принято: мягкий лимит (документирован в 03-api); синхронная резервация противоречит async-дизайну горячего пути |
| r15 | `rebuild-counters` держит ACCESS EXCLUSIVE на счётчики на весь агрегат — блокируются и редиректы ссылок с `max_clicks` | Принято: команда редкая, гонять в окно (06-deploy). «Staging-swap» ОТКЛОНЁН: теряет инкременты между снапшотом агрегата и TRUNCATE — текущая блокировка и есть гарантия корректности |
| r16 | PATCH `rules` у ссылки с большой историей кликов по вариантам: SET NULL-свип всей истории в транзакции запроса + ретроактивное стирание A/B-атрибуции | Отложено в бэклог (07-plans §10); на текущем масштабе не больно |
| r17 | `trustProxies` доверяет всем RFC1918: за Docker-шлюзом X-Forwarded-For спуфится (ip_address-условия, ignored sources, лимитер) | Отложено: сузить CIDR в проде до реального прокси (06-deploy); env-конфигурируемость — бэклог |
| r18 | Имя `TRACKING_DISABLE_PARAM` — фактически секрет: знание имени = обход трекинга и `max_clicks` | Принято: имя = ротируемый секрет (03-api/06-deploy) |
| r19 | Тестовые гэпы: guard схемы через вариант, SET NULL при PATCH с кликами, lock-abort двух prune-команд, 401-матрица v1 + `domain-groups`, зеркальная проверка окна, кап `visited_query`, точки в именах параметров | Фикс: +10 тестов (371 в сьюте) |

Открытым из аудита остаётся только: полная политика retention
`clicks`/`callbacks` (частично закрывается чистками этапа 2
[планов](./07-plans.md)) и ролевая модель за `users.is_admin` — при
появлении недоверенных операторов.
